/*console.log('hello world ---this is tutorial 6 for java script beginner lets go');

//Arrays : collection of variables is arrays, for example look below:;
// ------------------------------------------------------
let marks = [ 34,545,565,433,567];
const fruits = ['Apple','orange','banana','grapes','jackfruits','kiwi'];

//This is called ...(three dots) features in new java script ...followed by arrays names:

const mixed = [...fruits, ...marks];
console.log(mixed);

//or we can create arrays like this 
//note:while doing array constructor don't use square brackets use paranthesis;

const arr = new Array(34,43,65,3,'BholenathTagore');

console.log(arr);

//for index in fruits variable
console.log(fruits[3]);


//note example of properties and methods;

// this is the properties of arr 
console.log(arr.length); 
//this is the example of methods its shows true or false
console.log(Array.isArray(mixed)) ;

//we can change the value of the array like below 
//note methods can change the value of the array

arr[0] = 'shambhu';
console.log(arr);

// we can search by index value
/* here (index of method) Returns the index of the first occurrence of a value in an array, or -1 if it is not present.

@param searchElement — The value to locate in the array.

@param fromIndex — The array index at which to begin the search. If fromIndex is omitted, the search starts at index 0.*/

// let marks1 = marks.indexOf(2);
// console.log(marks1);

// //mutating or modifying Arrays
// //adding values at end of the arrays
// marks.push(34);
// console.log(marks)

// //adding values at start of the array
// marks.unshift(4345);
// console.log(marks);

// //subtracting a value at the end of the array
// marks.pop();
// console.log(marks);

// //subtracting a value at the starting of the array
// marks.shift();
// console.log(marks);

// //subtracting elements from 0 to 3
// marks.splice(0, 3);
// console.log(marks);

// //for reversing Arrays
// marks.reverse();
// console.log(marks);

// //concatenate values
// let marks3 = [1,3,4,67,45]
// marks = marks.concat(marks3);
// console.log(marks);
// //-----------------------------------------------------
// //lets do objects:
// //note in objects we can add booleans arrays numbers strng strings and all, values assigns by : not = 

// let myobj = {
//     name : 'hari',
//     channel : 'harekrishsna',
//     isActive: true,
//     marks: [2,34,53,6427, 4],

// }

// console.log(myobj);
// console.log(myobj.name);*/